export interface Job {
  id: string;
  job_name: string;
  status: string;
  total_cost: number;
  created_at: string; // Assuming date string for consistency
  // Add other properties as needed later
}
